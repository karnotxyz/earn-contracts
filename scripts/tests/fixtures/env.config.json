{
  "description": "Env-bound fixture config",
  "starknet": {
    "chainId": {
      "$env": "TEST_CHAIN_ID"
    },
    "rpcUrl": {
      "$env": "TEST_RPC_URL"
    },
    "accountAddress": {
      "$env": "TEST_DEPLOYER_ADDRESS"
    },
    "privateKey": {
      "$env": "TEST_DEPLOYER_PRIVATE_KEY"
    }
  },
  "factory": {
    "multisigAddress": {
      "$env": "TEST_MULTISIG_ADDRESS"
    },
    "upgradeDelaySeconds": {
      "$env": "TEST_UPGRADE_DELAY"
    }
  }
}
