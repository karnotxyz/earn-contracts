{
  "name": "earn-contracts-deploy",
  "version": "1.0.0",
  "description": "Karnot Earn factory deployment CLI",
  "type": "module",
  "scripts": {
    "deploy": "bun run cli.ts",
    "ui:account-demo": "bun run tools/serve-account-demo.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "starknet": "^9.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "bun-types": "^1.3.11",
    "typescript": "^6.0.3"
  }
}
